@model int
@{
    List<SelectListItem> _list  = new List<SelectListItem>();
    int endYear = DateTime.Now.Year - RP.GetCurrentSettings().MinimumAge + 3;
    int startYear = DateTime.Now.Year - RP.GetCurrentSettings().MaximumAge - 3;

    for (int i = endYear; i >= startYear; i = i -1)
    {
        _list.Add(new SelectListItem { Text = i.ToString(), Value = i.ToString(), Selected = i == Model });
    }
}
@Html.DropDownListFor(model => model, _list, new { @class = "drop_down" })
@model string
@{
    SelectList Items = new SelectList(System.Configuration.ConfigurationManager.AppSettings["BannerGroup"].Split(',').ToList(), Model);
}
@Html.DropDownListFor(model => model, Items)

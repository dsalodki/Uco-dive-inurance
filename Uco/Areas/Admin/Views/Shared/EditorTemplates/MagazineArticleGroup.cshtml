@model string
@{
    SelectList Items = new SelectList(System.Configuration.ConfigurationManager.AppSettings["MagazineArticleGroup"].Split(',').ToList(), Model);
}
@Html.DropDownListFor(model => model, Items, "----", null)
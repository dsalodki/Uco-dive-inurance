@model string
@{
    SelectList Items = new SelectList(System.Configuration.ConfigurationManager.AppSettings["ArticlesSpinnerSegments"].Split(',').ToList(), Model);
}
@Html.DropDownListFor(model => model, Items)
